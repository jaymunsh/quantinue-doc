# Task 2: in-memory app-order exposure reservation

## Scope

- Extended the typed reservation seam to return `accepted` plus an app-owned exposure summary.
- Stored per-order reference notional as `Decimal` and retained lifecycle state in the in-memory store.
- Kept daily-attempt accounting and exposure reservation inside the same store lock.
- Did not access Docker, localhost:5432, `.env`, provider credentials, or external broker APIs.

## Focused verification

| Check | Result |
| --- | --- |
| `uv run pytest -q tests/unit/test_daily_order_cap.py` | `11 passed` |
| `uv run ruff format --check src/quantinue/db/contracts.py src/quantinue/db/memory.py src/quantinue/roles/role_09_risk_portfolio/service.py tests/unit/test_daily_order_cap.py` | pass |
| `uv run ruff check src/quantinue/db/contracts.py src/quantinue/db/memory.py src/quantinue/roles/role_09_risk_portfolio/service.py tests/unit/test_daily_order_cap.py` | pass |
| `uv run basedpyright src/quantinue/db/contracts.py src/quantinue/db/memory.py src/quantinue/roles/role_09_risk_portfolio/service.py tests/unit/test_daily_order_cap.py` | `0 errors, 0 warnings, 0 notes` |
| `git diff --check` | pass |

The tests cover concurrent `600 + 600`, exact `600 + 400`, `999.99 + 0.01`, an additional-cent rejection, replay idempotency, account-wide exposure across dates, eligible lifecycle states, terminal release, stale-terminal-result protection, and retained daily-attempt quota.

## Manual store QA

Used an `anyio` minimal driver against `InMemoryRunStore`:

```text
{'first_accepted': True, 'second_accepted': False, 'after_first': '600.0', 'after_failure': '0'}
```

It reserved a `$600` order, rejected a second `$500` identity on the following date under the same `$1,000` account cap, then released the app exposure when the first order reconciled as `failed`.

## Cleanup and handoff

- No process, container, network, or secret artifact was created.
- Focused Python checks updated tracked `__pycache__` artifacts. This worker restored only those seven generated bytecode files and confirmed that no `*.pyc` path remains in the diff.
- PostgreSQL parity, provider-status mapping, pipeline cap-rejection wording, and dashboard rendering remain owned by later tasks.
